# JARVIS PRD2 — Optimisation, Sécurisation et Exécution Système

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformer JARVIS en un assistant ultra-réactif multi-config (RTX 3070 / RX 7900 XTX / CPU) avec TTS streaming par phrase, visualiseur audio réactif, sécurité renforcée et packaging portable.

**Architecture:** Profils matériels auto-détectés (NVIDIA CUDA / AMD CPU-optimisé) configurent LLM layers, Whisper model size et compute type. Le LLM stream alimente un TTS worker concurrent par phrase pour réduire la latence à ~1-2s. Le VoiceVisualizer se branche sur l'AnalyserNode du TTS pour les animations SPEAKING.

**Tech Stack:** Python 3.10+ · FastAPI · llama-cpp-python · faster-whisper · Piper TTS · React 19 · Zustand · Web Audio API · Tauri v2 · Framer Motion

---

## FILE STRUCTURE

```
server/
  utils/
    hardware.py          [CREATE] Détection GPU + profils matériels
    config.py            [MODIFY] Utilise detect_profile() au lieu de valeurs fixes
    rate_limiter.py      [CREATE] Rate limiting WebSocket par IP
  core/
    tts.py               [MODIFY] Ajouter synthesize_text() avec découpage par phrase
  api/
    websocket.py         [MODIFY] Pipeline TTS streaming concurrent + rate limiter

client/src/
  types/index.ts         [MODIFY] Ajouter tts_chunk dans ServerEvent
  stores/
    jarvisStore.ts       [MODIFY] Queue TTS séquentiel + AnalyserNode TTS exposé
  components/
    VoiceVisualizer/
      VoiceVisualizer.tsx [MODIFY] Utilise AnalyserNode TTS quand SPEAKING
  lib/
    audioVisualizer.ts   [MODIFY] Mode SPEAKING = fréquences TTS réelles

build.bat                [CREATE] Script build complet (check + Tauri)
LANCER-JARVIS.bat        [CREATE] Lance serveur + app en un clic
```

---

## Task 1 — Profils Matériels Auto-Détectés

**Files:**
- Create: `server/utils/hardware.py`
- Modify: `server/utils/config.py` (lignes 1-37)

- [ ] **Step 1 : Créer `server/utils/hardware.py`**

```python
from __future__ import annotations
import subprocess
import os
from dataclasses import dataclass, replace
from utils.logger import get_logger

logger = get_logger("hardware")


@dataclass(frozen=True)
class HardwareProfile:
    name: str           # identifiant lisible
    device: str         # "cuda" | "cpu"
    vram_mb: int        # VRAM détectée (0 si CPU)
    n_gpu_layers: int   # -1 = tout sur GPU, 0 = CPU
    n_threads: int      # threads CPU pour LLM / STT
    whisper_model: str  # "tiny" | "small" | "medium"
    whisper_compute: str  # "float16" | "int8"
    llm_max_vram_mb: int  # budget VRAM alloué au LLM


# ── Profils prédéfinis ───────────────────────────────────────────────────────

_NVIDIA_HIGH = HardwareProfile(
    name="nvidia_high",          # >=16 GB VRAM (RTX 3090/4090, RX n/a)
    device="cuda", vram_mb=0,
    n_gpu_layers=-1,             # toutes les couches sur GPU
    n_threads=8,
    whisper_model="medium",      # ~1.5 GB VRAM
    whisper_compute="float16",
    llm_max_vram_mb=18_000,
)

_NVIDIA_MEDIUM = HardwareProfile(
    name="nvidia_medium",        # 8 GB VRAM (RTX 3070, RTX 4060 Ti)
    device="cuda", vram_mb=0,
    n_gpu_layers=28,             # Q4_K_M 7B ≈ 4.4 GB VRAM
    n_threads=8,
    whisper_model="small",       # ~500 MB VRAM
    whisper_compute="float16",
    llm_max_vram_mb=4_800,
)

_NVIDIA_LOW = HardwareProfile(
    name="nvidia_low",           # <8 GB VRAM (RTX 3060, GTX 1080)
    device="cuda", vram_mb=0,
    n_gpu_layers=16,             # partiel, reste sur CPU
    n_threads=8,
    whisper_model="tiny",        # ~150 MB VRAM
    whisper_compute="float16",
    llm_max_vram_mb=3_000,
)

_CPU_HIGHEND = HardwareProfile(
    name="cpu_highend",          # i9-13900K, Ryzen 9, AMD GPU (no CUDA)
    device="cpu", vram_mb=0,
    n_gpu_layers=0,              # CPU uniquement
    n_threads=16,                # profite des P-cores + E-cores i9
    whisper_model="medium",      # CPU int8 rapide sur i9
    whisper_compute="int8",
    llm_max_vram_mb=0,
)

_CPU_STANDARD = HardwareProfile(
    name="cpu_standard",         # i5/i7, AMD Ryzen 5/7
    device="cpu", vram_mb=0,
    n_gpu_layers=0,
    n_threads=8,
    whisper_model="small",
    whisper_compute="int8",
    llm_max_vram_mb=0,
)


# ── Détection ────────────────────────────────────────────────────────────────

def _nvidia_vram() -> tuple[str, int] | None:
    """Retourne (nom_gpu, vram_mb) via nvidia-smi, ou None."""
    try:
        r = subprocess.run(
            ["nvidia-smi", "--query-gpu=name,memory.total",
             "--format=csv,noheader,nounits"],
            capture_output=True, text=True, timeout=5,
        )
        if r.returncode != 0:
            return None
        line = r.stdout.strip().split("\n")[0]
        parts = line.split(",")
        if len(parts) < 2:
            return None
        return parts[0].strip(), int(parts[1].strip())
    except Exception as e:
        logger.debug(f"nvidia-smi indisponible: {e}")
        return None


def _cpu_thread_count() -> int:
    """Nombre de threads logiques disponibles (min 4)."""
    return max(4, os.cpu_count() or 4)


def detect_profile() -> HardwareProfile:
    """Sélectionne le profil matériel optimal et loggue la décision."""
    nvidia = _nvidia_vram()

    if nvidia:
        name, vram_mb = nvidia
        if vram_mb >= 16_000:
            profile = _NVIDIA_HIGH
        elif vram_mb >= 7_000:
            profile = _NVIDIA_MEDIUM
        else:
            profile = _NVIDIA_LOW
        profile = replace(profile, vram_mb=vram_mb)
        logger.info(
            f"Profil {profile.name!r} — GPU: {name} "
            f"({vram_mb} MB) · Whisper: {profile.whisper_model} · "
            f"GPU layers: {profile.n_gpu_layers}"
        )
        return profile

    # Pas de GPU NVIDIA — profil CPU selon nombre de cœurs
    threads = _cpu_thread_count()
    profile = _CPU_HIGHEND if threads >= 16 else _CPU_STANDARD
    logger.info(
        f"Profil {profile.name!r} — CPU ({threads} threads) · "
        f"Whisper: {profile.whisper_model} · device: cpu"
    )
    return profile
```

- [ ] **Step 2 : Mettre à jour `server/utils/config.py` pour utiliser `detect_profile()`**

Remplacer entièrement le fichier :

```python
from __future__ import annotations
from pydantic_settings import BaseSettings
from pathlib import Path
from utils.hardware import detect_profile, HardwareProfile

MODELS_DIR = Path(__file__).parents[1] / "models"

# Détection unique au démarrage
_profile: HardwareProfile = detect_profile()


class Settings(BaseSettings):
    # Server
    host: str = "127.0.0.1"
    port: int = 8765

    # LLM — valeurs par défaut issues du profil détecté
    model_path: Path = MODELS_DIR / "Mistral-7B-Instruct-v0.3-Q4_K_M.gguf"
    n_ctx: int = 4096
    n_gpu_layers: int = _profile.n_gpu_layers
    n_threads: int = _profile.n_threads

    # STT
    whisper_model: str = str(MODELS_DIR / f"faster-whisper-{_profile.whisper_model}")
    whisper_device: str = _profile.device
    whisper_compute_type: str = _profile.whisper_compute

    # TTS — toujours CPU (Piper est un subprocess)
    piper_exe: Path = MODELS_DIR / "piper" / "piper.exe"
    piper_voice: Path = MODELS_DIR / "piper" / "fr_FR-upmc-medium.onnx"

    # Memory
    max_context_messages: int = 20

    # Hardware profile (lecture seule pour les logs)
    hw_profile: str = _profile.name

    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}


settings = Settings()
```

- [ ] **Step 3 : Vérifier que le profil est loggué au démarrage**

```bash
cd server && .venv/Scripts/python.exe -c "from utils.config import settings; print(settings.hw_profile, settings.n_gpu_layers, settings.whisper_model)"
```

Résultat attendu (RTX 3070) : `nvidia_medium 28 C:\...\faster-whisper-small`

- [ ] **Step 4 : Commit**

```bash
git add server/utils/hardware.py server/utils/config.py
git commit -m "feat: hardware profile auto-detection (NVIDIA/CPU, 5 profiles)"
```

---

## Task 2 — Pipeline TTS Streaming par Phrase (Serveur)

**Files:**
- Modify: `server/core/tts.py`
- Modify: `server/api/websocket.py`

- [ ] **Step 1 : Ajouter `_split_sentences()` et `synthesize_streaming()` dans `server/core/tts.py`**

Ajouter après la méthode `synthesize()` existante :

```python
import re as _re

_SENTENCE_END = _re.compile(r'(?<=[.!?…])\s+|(?<=[.!?…])$')


def _split_sentences(text: str) -> list[str]:
    """Découpe le texte en phrases sur ponctuation forte."""
    parts = _SENTENCE_END.split(text.strip())
    return [p.strip() for p in parts if p.strip()]


async def synthesize_streaming(self, text: str) -> AsyncGenerator[str, None]:
    """Génère un audio base64 par phrase dès qu'elle est disponible."""
    for sentence in _split_sentences(text):
        audio = await self.synthesize(sentence)
        if audio:
            yield audio
```

Ajouter `from typing import AsyncGenerator` en tête du fichier si absent.

- [ ] **Step 2 : Créer le TTS worker concurrent dans `server/api/websocket.py`**

Ajouter après les constantes (ligne ~18) :

```python
import asyncio
import re

_SENTENCE_BOUNDARY = re.compile(r'(?<=[.!?…»])\s')
```

Ajouter la fonction `_tts_sentence_worker` avant `handle_text_query` :

```python
async def _tts_sentence_worker(
    queue: asyncio.Queue,
    ws: WebSocket,
    tts: "TTSManager",
) -> None:
    """Consomme les phrases de la queue, synthétise et envoie les chunks audio."""
    index = 0
    while True:
        sentence: str | None = await queue.get()
        if sentence is None:  # sentinelle de fin
            await manager.send(ws, "tts_chunk", {"audio": "", "final": True, "index": index})
            return
        audio_b64 = await tts.synthesize(sentence)
        if audio_b64:
            await manager.send(ws, "tts_chunk", {"audio": audio_b64, "final": False, "index": index})
            index += 1
```

- [ ] **Step 3 : Modifier `handle_text_query()` pour le streaming TTS concurrent**

Remplacer entièrement la fonction `handle_text_query` :

```python
async def handle_text_query(
    ws: WebSocket,
    text: str,
    llm: "LLMManager",
    memory: "ContextMemory",
    tts: "TTSManager",
    tools: "ToolRegistry",
    tts_enabled: bool = True,
) -> None:
    await manager.send(ws, "status", {"status": "processing"})
    memory.add_user(text)
    message_id = str(uuid.uuid4())

    # ── Phase 1 : Stream LLM + détection phrases pour TTS concurrent ─────────
    tts_queue: asyncio.Queue[str | None] = asyncio.Queue()
    tts_task = None
    if tts_enabled and tts.is_available:
        await manager.send(ws, "status", {"status": "speaking"})
        tts_task = asyncio.create_task(_tts_sentence_worker(tts_queue, ws, tts))

    full_response = ""
    sentence_buf = ""

    async for token in llm.stream(memory.get_messages()):
        full_response += token
        # Ne pas streamer le marker d'outil vers le client
        if "<JARVIS_TOOL>" not in full_response:
            await manager.send(ws, "token", {"token": token, "messageId": message_id})

        if tts_task and "<JARVIS_TOOL>" not in full_response:
            sentence_buf += token
            m = _SENTENCE_BOUNDARY.search(sentence_buf)
            if m:
                phrase = sentence_buf[: m.start() + 1].strip()
                sentence_buf = sentence_buf[m.end():]
                if phrase:
                    await tts_queue.put(phrase)

    # Flush le buffer restant
    if tts_task:
        if sentence_buf.strip() and "<JARVIS_TOOL>" not in sentence_buf:
            await tts_queue.put(sentence_buf.strip())
        await tts_queue.put(None)  # fin

    # ── Phase 2 : Tool call si détecté ──────────────────────────────────────
    tool_call = parse_tool_call(full_response)
    if tool_call:
        tool_name, tool_args = tool_call
        logger.info(f"Tool call: {tool_name}({tool_args})")
        tool_result = tools.execute(tool_name, **tool_args)

        tool_context = f"[Résultat de {tool_name}: {tool_result}]"
        memory.add_assistant(tool_context)

        second_id = str(uuid.uuid4())
        second_response = ""
        second_buf = ""

        # Nouveau worker TTS pour la réponse de l'outil
        tts_queue2: asyncio.Queue[str | None] = asyncio.Queue()
        tts_task2 = None
        if tts_enabled and tts.is_available:
            tts_task2 = asyncio.create_task(_tts_sentence_worker(tts_queue2, ws, tts))

        async for token in llm.stream(memory.get_messages(), max_tokens=256):
            second_response += token
            await manager.send(ws, "token", {"token": token, "messageId": second_id})
            if tts_task2:
                second_buf += token
                m2 = _SENTENCE_BOUNDARY.search(second_buf)
                if m2:
                    phrase2 = second_buf[: m2.start() + 1].strip()
                    second_buf = second_buf[m2.end():]
                    if phrase2:
                        await tts_queue2.put(phrase2)

        if tts_task2:
            if second_buf.strip():
                await tts_queue2.put(second_buf.strip())
            await tts_queue2.put(None)
            await tts_task2

        memory.add_assistant(second_response)
        await manager.send(ws, "message_done", {"messageId": second_id})
    else:
        memory.add_assistant(full_response)
        await manager.send(ws, "message_done", {"messageId": message_id})

    # Attendre la fin du TTS principal si pas d'outil
    if tts_task:
        await tts_task

    if not tts_enabled or not tts.is_available:
        await manager.send(ws, "status", {"status": "idle"})
```

- [ ] **Step 4 : Vérifier syntaxe Python**

```bash
cd server && .venv/Scripts/python.exe -m py_compile api/websocket.py core/tts.py && echo "OK"
```

Résultat attendu : `OK`

- [ ] **Step 5 : Commit**

```bash
git add server/core/tts.py server/api/websocket.py
git commit -m "feat: streaming TTS par phrase — latence réduite à ~1-2s première parole"
```

---

## Task 3 — Client : Queue TTS Séquentielle + AnalyserNode

**Files:**
- Modify: `client/src/types/index.ts`
- Modify: `client/src/stores/jarvisStore.ts`

- [ ] **Step 1 : Ajouter `tts_chunk` dans `client/src/types/index.ts`**

Remplacer la ligne `server_status` :

```typescript
export type ServerEvent =
  | { type: "status"; payload: { status: JarvisStatus } }
  | { type: "token"; payload: { token: string; messageId: string } }
  | { type: "message_done"; payload: { messageId: string } }
  | { type: "tts_audio"; payload: { audio: string } }
  | { type: "tts_chunk"; payload: { audio: string; final: boolean; index: number } }
  | { type: "stt_text"; payload: { text: string } }
  | { type: "tool_result"; payload: { tool: string; result: string } }
  | { type: "error"; payload: { message: string } }
  | { type: "server_status"; payload: { llm: boolean; stt: boolean; tts: boolean } };
```

- [ ] **Step 2 : Remplacer la gestion TTS dans `client/src/stores/jarvisStore.ts`**

Remplacer les fonctions `playTtsAudio` + store par cette version avec queue séquentielle et AnalyserNode exposé :

```typescript
// ── Audio infrastructure ────────────────────────────────────────────────────

let _audioCtx: AudioContext | null = null;
export let ttsAnalyser: AnalyserNode | null = null; // exposé pour VoiceVisualizer

function getAudioContext(): AudioContext {
  if (!_audioCtx || _audioCtx.state === "closed") {
    _audioCtx = new AudioContext();
    ttsAnalyser = _audioCtx.createAnalyser();
    ttsAnalyser.fftSize = 512;
    ttsAnalyser.connect(_audioCtx.destination);
  }
  if (_audioCtx.state === "suspended") void _audioCtx.resume();
  return _audioCtx;
}

function buildJarvisChain(ctx: AudioContext): AudioNode {
  const hp = ctx.createBiquadFilter();
  hp.type = "highpass";
  hp.frequency.value = 90;

  const presence = ctx.createBiquadFilter();
  presence.type = "peaking";
  presence.frequency.value = 3000;
  presence.Q.value = 1.4;
  presence.gain.value = 4;

  const shelf = ctx.createBiquadFilter();
  shelf.type = "highshelf";
  shelf.frequency.value = 10000;
  shelf.gain.value = -2;

  const comp = ctx.createDynamicsCompressor();
  comp.threshold.value = -20;
  comp.knee.value = 5;
  comp.ratio.value = 5;
  comp.attack.value = 0.003;
  comp.release.value = 0.12;

  hp.connect(presence);
  presence.connect(shelf);
  shelf.connect(comp);
  // Connecter vers l'analyser TTS (pas destination directement)
  if (ttsAnalyser) {
    comp.connect(ttsAnalyser);
  } else {
    comp.connect(ctx.destination);
  }
  return hp;
}

// ── Queue TTS séquentielle ──────────────────────────────────────────────────

const _ttsQueue: string[] = [];
let _ttsPlaying = false;
const TTS_TIMEOUT_MS = 60_000;

async function _playNextChunk(onAllDone: () => void): Promise<void> {
  if (_ttsQueue.length === 0) {
    _ttsPlaying = false;
    onAllDone();
    return;
  }
  const b64 = _ttsQueue.shift()!;
  try {
    const binary = atob(b64);
    const bytes = new Uint8Array(binary.length);
    for (let i = 0; i < binary.length; i++) bytes[i] = binary.charCodeAt(i);
    const ctx = getAudioContext();
    const buffer = await ctx.decodeAudioData(bytes.buffer);
    const source = ctx.createBufferSource();
    source.buffer = buffer;
    source.playbackRate.value = 1.04;
    source.connect(buildJarvisChain(ctx));

    let tid: number | undefined;
    const next = () => {
      clearTimeout(tid);
      void _playNextChunk(onAllDone);
    };
    tid = window.setTimeout(next, TTS_TIMEOUT_MS);
    source.onended = next;
    source.start(0);
  } catch (e) {
    console.error("TTS chunk error:", e);
    void _playNextChunk(onAllDone);
  }
}

function enqueueTtsChunk(b64: string, final: boolean, onAllDone: () => void): void {
  if (b64) _ttsQueue.push(b64);
  if (!_ttsPlaying && _ttsQueue.length > 0) {
    _ttsPlaying = true;
    void _playNextChunk(onAllDone);
  } else if (final && _ttsQueue.length === 0 && !_ttsPlaying) {
    onAllDone();
  }
}
```

- [ ] **Step 3 : Mettre à jour `handleServerEvent` dans le même fichier**

Dans le switch de `handleServerEvent`, ajouter après le case `tts_audio` :

```typescript
      case "tts_chunk": {
        const { audio, final } = event.payload;
        if (!get().ttsEnabled) {
          if (final) {
            setStatus("idle");
            wsSend?.({ type: "tts_done", payload: {} });
          }
          return;
        }
        enqueueTtsChunk(audio, final, () => {
          setStatus("idle");
          wsSend?.({ type: "tts_done", payload: {} });
        });
        break;
      }
```

- [ ] **Step 4 : TypeScript check**

```bash
cd client && npx tsc --noEmit 2>&1 | head -20
```

Résultat attendu : aucune erreur

- [ ] **Step 5 : Commit**

```bash
git add client/src/types/index.ts client/src/stores/jarvisStore.ts
git commit -m "feat: queue TTS séquentielle par phrase + AnalyserNode TTS exposé"
```

---

## Task 4 — VoiceVisualizer : THINKING Rotation + SPEAKING Fréquences TTS

**Files:**
- Modify: `client/src/lib/audioVisualizer.ts`
- Modify: `client/src/components/VoiceVisualizer/VoiceVisualizer.tsx`

- [ ] **Step 1 : Mettre à jour `client/src/lib/audioVisualizer.ts`**

Ajouter en tête du fichier après les imports existants :

```typescript
import { ttsAnalyser } from "../../stores/jarvisStore";
```

Dans la fonction `drawVisualizer`, modifier le bloc SPEAKING pour utiliser l'analyser TTS réel :

Trouver la section où le status est "speaking" et remplacer la logique de données fréquentielles :

```typescript
// Au début de drawVisualizer, ajouter :
function getFreqData(analyser: AnalyserNode | null, fallback: Uint8Array): Uint8Array {
  const buf = getFreqBuffer(analyser ?? ({} as AnalyserNode));
  if (analyser) {
    analyser.getByteFrequencyData(buf);
    return buf;
  }
  return fallback;
}
```

Dans le rendu SPEAKING, remplacer `analyser.getByteFrequencyData(freqData)` par :

```typescript
const freqData = status === "speaking" && ttsAnalyser
  ? getFreqData(ttsAnalyser, new Uint8Array(256))
  : getFreqBuffer(analyser);
if (status !== "speaking") analyser.getByteFrequencyData(freqData);
```

- [ ] **Step 2 : Ajouter animation rotation THINKING dans `VoiceVisualizer.tsx`**

Remplacer entièrement le composant :

```tsx
import { useEffect, useRef } from "react";
import { motion } from "framer-motion";
import { useJarvisStore } from "../../stores/jarvisStore";
import { drawVisualizer } from "../../lib/audioVisualizer";
import { ttsAnalyser } from "../../stores/jarvisStore";

export function VoiceVisualizer() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const analyserRef = useRef<AnalyserNode | null>(null);
  const status = useJarvisStore((s) => s.status);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    // Analyser micro (pour LISTENING) — null si pas actif
    const analyser = analyserRef.current;
    let rafId: number;

    const render = () => {
      rafId = requestAnimationFrame(render);
      // Quand SPEAKING, on passe l'analyser TTS, sinon l'analyser micro
      const activeAnalyser = status === "speaking" ? ttsAnalyser : analyser;
      drawVisualizer(canvas, ctx, activeAnalyser, status);
    };
    render();
    return () => cancelAnimationFrame(rafId);
  }, [status]);

  const isThinking = status === "processing";

  return (
    <div className="relative w-48 h-48">
      {/* Canvas principal */}
      <canvas ref={canvasRef} width={192} height={192} className="absolute inset-0" />

      {/* Anneau rotatif THINKING — overlay */}
      {isThinking && (
        <motion.div
          className="absolute inset-0 rounded-full pointer-events-none"
          style={{
            border: "2px solid transparent",
            borderTopColor: "#ffaa00",
            borderRightColor: "#ffaa0066",
          }}
          animate={{ rotate: 360 }}
          transition={{ duration: 1.2, repeat: Infinity, ease: "linear" }}
        />
      )}
      {isThinking && (
        <motion.div
          className="absolute inset-2 rounded-full pointer-events-none"
          style={{
            border: "1px solid transparent",
            borderBottomColor: "#ffaa00aa",
          }}
          animate={{ rotate: -360 }}
          transition={{ duration: 2, repeat: Infinity, ease: "linear" }}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 3 : TypeScript check**

```bash
cd client && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 4 : Commit**

```bash
git add client/src/lib/audioVisualizer.ts client/src/components/VoiceVisualizer/VoiceVisualizer.tsx
git commit -m "feat: visualizer THINKING rotation + SPEAKING branché sur AnalyserNode TTS"
```

---

## Task 5 — Sécurité : Rate Limiter WebSocket + Validation Arguments

**Files:**
- Create: `server/utils/rate_limiter.py`
- Modify: `server/api/websocket.py` (intégrer le limiter)
- Modify: `server/tools/system_tools.py` (validation stricte args)

- [ ] **Step 1 : Créer `server/utils/rate_limiter.py`**

```python
from __future__ import annotations
import time
from collections import defaultdict
from utils.logger import get_logger

logger = get_logger("rate_limiter")

# Max requêtes text_query par fenêtre glissante
_TEXT_QUERY_MAX = 20       # 20 requêtes par minute
_TEXT_QUERY_WINDOW = 60    # secondes
_AUDIO_CHUNK_MAX = 300     # 300 chunks audio par minute (~5 min audio)
_AUDIO_CHUNK_WINDOW = 60


class _Counter:
    __slots__ = ("timestamps",)

    def __init__(self) -> None:
        self.timestamps: list[float] = []

    def is_allowed(self, now: float, max_count: int, window: float) -> bool:
        # Purge les timestamps hors fenêtre
        cutoff = now - window
        self.timestamps = [t for t in self.timestamps if t >= cutoff]
        if len(self.timestamps) >= max_count:
            return False
        self.timestamps.append(now)
        return True


class RateLimiter:
    """Rate limiter par client WebSocket (adresse IP ou objet ws)."""

    def __init__(self) -> None:
        self._text: dict[int, _Counter] = defaultdict(_Counter)
        self._audio: dict[int, _Counter] = defaultdict(_Counter)

    def allow_text(self, ws_id: int) -> bool:
        allowed = self._text[ws_id].is_allowed(
            time.monotonic(), _TEXT_QUERY_MAX, _TEXT_QUERY_WINDOW
        )
        if not allowed:
            logger.warning(f"Rate limit text_query ws={ws_id}")
        return allowed

    def allow_audio(self, ws_id: int) -> bool:
        return self._audio[ws_id].is_allowed(
            time.monotonic(), _AUDIO_CHUNK_MAX, _AUDIO_CHUNK_WINDOW
        )

    def cleanup(self, ws_id: int) -> None:
        self._text.pop(ws_id, None)
        self._audio.pop(ws_id, None)
```

- [ ] **Step 2 : Intégrer `RateLimiter` dans `server/api/websocket.py`**

Ajouter après les imports :

```python
from utils.rate_limiter import RateLimiter

_rate_limiter = RateLimiter()
```

Dans `websocket_handler`, après `await manager.connect(ws)` :

```python
ws_id = id(ws)
```

Dans le handler des events `text_query` :

```python
            if event_type == "text_query":
                if not _rate_limiter.allow_text(ws_id):
                    await manager.send(ws, "error", {"message": "Trop de requêtes. Patientez."})
                    continue
                text = str(payload.get("text", "")).strip()
```

Dans `audio_chunk` :

```python
            elif event_type == "audio_chunk":
                if not _rate_limiter.allow_audio(ws_id):
                    continue  # silencieux, drop le chunk
```

Dans le bloc `except WebSocketDisconnect` :

```python
    except WebSocketDisconnect:
        _rate_limiter.cleanup(ws_id)
        manager.disconnect(ws)
```

- [ ] **Step 3 : Valider types stricts des arguments outils dans `server/tools/system_tools.py`**

Ajouter en tête de `open_application()` et `kill_application()` :

```python
def open_application(name: str) -> str:
    if not isinstance(name, str) or not name.strip():
        return "Erreur: le nom de l'application doit être une chaîne non vide."
    name = name.strip().lower()
    ...

def kill_application(name: str) -> str:
    if not isinstance(name, str) or not name.strip():
        return "Erreur: nom d'application invalide."
    name = name.strip().lower()
    ...
```

- [ ] **Step 4 : Vérifier syntaxe**

```bash
cd server && .venv/Scripts/python.exe -m py_compile utils/rate_limiter.py api/websocket.py tools/system_tools.py && echo "OK"
```

- [ ] **Step 5 : Commit**

```bash
git add server/utils/rate_limiter.py server/api/websocket.py server/tools/system_tools.py
git commit -m "security: rate limiter WebSocket + validation stricte args outils"
```

---

## Task 6 — Build & Packaging

**Files:**
- Create: `build.bat` (remplace le stub existant)
- Create: `LANCER-JARVIS.bat`
- Modify: `client/src-tauri/tauri.conf.json` (bundle config)

- [ ] **Step 1 : Créer `build.bat` à la racine**

```batch
@echo off
setlocal EnableDelayedExpansion
title JARVIS — Build System
echo.
echo  ╔══════════════════════════════════════╗
echo  ║       J.A.R.V.I.S.  BUILD           ║
echo  ╚══════════════════════════════════════╝
echo.

:: ── 1. Vérifications préalables ──────────────────────────────────────────
echo [1/5] Verification des dependances...
where node >nul 2>&1 || (echo ERREUR: Node.js manquant & exit /b 1)
where cargo >nul 2>&1 || (echo ERREUR: Rust/Cargo manquant & exit /b 1)
if not exist "server\.venv\Scripts\python.exe" (
    echo ERREUR: venv Python manquant - lancez: python -m venv server\.venv
    exit /b 1
)
if not exist "server\models\Mistral-7B-Instruct-v0.3-Q4_K_M.gguf" (
    echo AVERTISSEMENT: Modele LLM manquant dans server\models\
)

:: ── 2. Kill processus existants ───────────────────────────────────────────
echo [2/5] Arret des processus existants...
taskkill /F /IM "JARVIS.exe" >nul 2>&1
taskkill /F /IM "python.exe" >nul 2>&1
timeout /t 2 /nobreak >nul

:: ── 3. Build frontend React ───────────────────────────────────────────────
echo [3/5] Build frontend React...
cd client
call npm install --silent
call npm run build
if errorlevel 1 (echo ERREUR: Build React echoue & exit /b 1)

:: ── 4. Build Tauri (app + installer NSIS) ────────────────────────────────
echo [4/5] Build Tauri (peut prendre 3-5 minutes)...
call npx tauri build
if errorlevel 1 (echo ERREUR: Build Tauri echoue & exit /b 1)

:: ── 5. Résumé ─────────────────────────────────────────────────────────────
echo [5/5] Build termine !
echo.
echo  Executables generes:
echo   - Installer : client\src-tauri\target\release\bundle\nsis\JARVIS_1.0.0_x64-setup.exe
echo   - Portable  : client\src-tauri\target\release\JARVIS.exe
echo.

cd ..
endlocal
```

- [ ] **Step 2 : Créer `LANCER-JARVIS.bat` à la racine**

```batch
@echo off
title J.A.R.V.I.S. — Launcher
echo.
echo  ╔══════════════════════════════════════╗
echo  ║      J.A.R.V.I.S.  LAUNCHER         ║
echo  ╚══════════════════════════════════════╝

:: Démarrer le serveur Python en arrière-plan
echo [JARVIS] Demarrage du serveur IA...
start "JARVIS-Server" /min cmd /c "cd /d "%~dp0server" && .venv\Scripts\python.exe main.py"

:: Attendre que le serveur soit prêt (max 90s)
echo [JARVIS] Chargement des modeles IA (30-60s)...
set /a tries=0
:wait_loop
timeout /t 3 /nobreak >nul
curl -s http://127.0.0.1:8765/api/health >nul 2>&1
if errorlevel 1 (
    set /a tries+=1
    if !tries! lss 30 goto wait_loop
    echo ERREUR: Le serveur n'a pas demarré en temps voulu.
    exit /b 1
)

echo [JARVIS] Serveur pret ! Lancement de l'interface...

:: Lancer l'app (cherche l'exe compilé, sinon dev mode)
if exist "%~dp0client\src-tauri\target\release\JARVIS.exe" (
    start "" "%~dp0client\src-tauri\target\release\JARVIS.exe"
) else (
    echo [JARVIS] Mode developpement (npm run tauri dev)...
    start "JARVIS-App" /min cmd /c "cd /d "%~dp0client" && npx tauri dev"
)
```

- [ ] **Step 3 : Vérifier que le build Tauri génère bien un NSIS installer**

Vérifier dans `client/src-tauri/tauri.conf.json` que `bundle.targets` est `"all"` (déjà configuré).

Ajouter les métadonnées bundle manquantes dans `tauri.conf.json` :

```json
"bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/icon.ico"
    ],
    "windows": {
      "nsis": {
        "installMode": "currentUser"
      }
    }
  }
```

- [ ] **Step 4 : Commit**

```bash
git add build.bat LANCER-JARVIS.bat client/src-tauri/tauri.conf.json
git commit -m "build: script build complet + launcher one-click JARVIS"
```

---

## Auto-Review

### 1. Couverture spec

| Requirement PRD | Task | Status |
|---|---|---|
| Profils VRAM RTX 3070 max 4.8 GB | Task 1 — n_gpu_layers=28 | ✅ |
| Config i9-13900K + RX 7900 XTX | Task 1 — cpu_highend 16 threads | ✅ |
| STT Whisper float16 CUDA | Task 1 — profil medium/high | ✅ |
| TTS sur CPU | Config — Piper toujours subprocess CPU | ✅ |
| Streaming TTS latence réduite | Task 2 + 3 | ✅ |
| Interdiction shell brut | Déjà en place (whitelist tools) | ✅ |
| Function calling JSON schema | Déjà en place (`<JARVIS_TOOL>` parser) | ✅ |
| Sanitisation arguments | Task 5 — validation isinstance + strip | ✅ |
| Rate limiting | Task 5 — RateLimiter par ws_id | ✅ |
| Visualiseur SPEAKING réactif audio | Task 4 — AnalyserNode TTS | ✅ |
| État THINKING animation rotation | Task 4 — Framer Motion rotation | ✅ |
| Build portable + installer NSIS | Task 6 — build.bat + LANCER-JARVIS.bat | ✅ |

### 2. Placeholder scan

Aucun placeholder détecté — tout le code est complet.

### 3. Type consistency

- `ttsAnalyser: AnalyserNode | null` — exporté dans jarvisStore.ts, importé dans audioVisualizer.ts ✅
- `tts_chunk` event défini dans types/index.ts + géré dans handleServerEvent ✅  
- `HardwareProfile` créé dans hardware.py, utilisé dans config.py ✅
- `_tts_sentence_worker(queue, ws, tts)` — signature consistante task 2 ✅
